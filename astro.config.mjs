import { defineConfig } from 'astro/config';

// User site deployed at the domain root (raphaelp92.github.io) → base stays '/'.
export default defineConfig({
  site: 'https://raphaelp92.github.io',
});
